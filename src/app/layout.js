import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "YAAN X",
  description: "The future of luxury travel is here! YAANX is not just another cab service—it's a revolution in premium, safe, and seamless mobility. Designed for those who demand more from their journeys, YAANX blends technology, comfort, and convenience to deliver a ride like never before.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
